[output."DP-1"]
enabled = true
mode = "1920x1080@144"
position = [0, 0]
scale = 1

[output."HDMI-A-1"]
enabled = true
mode = "1920x1080@144"
position = [1920, 30]
scale = 1

# vrr = "fullscreen"            # "disabled" (default), "always", or "fullscreen"
# hdr = "auto"                  # "off", "on", metadata-aware "auto", or any fullscreen surface with "fullscreen"
# sdr_white = 203                # SDR reference white in cd/m2 while HDR is active
# Dynamic workspaces are the default; use a count or name list for static workspaces.
# workspaces = 5
