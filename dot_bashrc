#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

# aliases
alias cd='z'
alias ls='exa -lah'
alias nf='nerdfetch'
alias pf='pfetch'
alias ff='fastfetch'
alias v='nvim'
alias gg='lazygit'
alias tree='tre -e automatic'
alias cz='chezmoi'
alias k='kubectl'

# git
alias gs='git status'
alias ga='git add'
alias gaa='git add .'
alias gc='git commit'
alias gcm='git commit -m'
alias gp='git push'
alias gpl='git pull'
alias gi='git init'
alias gcl='git clone'

# branching
alias gb='git branch'
alias gba='git branch -a'
alias gco='git checkout'
alias gcb='git checkout -b'
alias gsw='git switch'
alias gswc='git switch -c'

# logs and diffs
alias gl='git log --oneline --graph --decorate'
alias gd='git diff'
alias gds='git diff --staged'

# functions
function y() {
	local tmp cwd; tmp="$(mktemp -t "yazi-cwd.XXXXXX")"
	command yazi "$@" --cwd-file="$tmp"
	IFS= read -r -d '' cwd < "$tmp"
	[ "$cwd" != "$PWD" ] && [ -d "$cwd" ] && builtin cd -- "$cwd" || builtin true
	command rm -f -- "$tmp"
}

export PS1='\e[01;32m\u@\h\e[00m:\e[01;34m\w\e[00m\n> '
export RPS1='\e[01;33m\t\e[00m'
export LANG=en_IN.UTF-8
export PATH="$HOME/go/bin:$PATH"

enable flyline 2>/dev/null || enable -f /usr/lib/bash/libflyline.so flyline

### flyline config
flyline key bind Ctrl+f inlineSuggestionAvailable=inlineSuggestionAccept
flyline key bind Ctrl+n always=nextHistoryEntry
flyline key bind Ctrl+p always=prevHistoryEntry
flyline key bind Ctrl+n fuzzyHistorySearch=fuzzyHistorySelectNext
flyline key bind Ctrl+p fuzzyHistorySearch=fuzzyHistorySelectPrev
flyline key bind Ctrl+n tabCompletionAvailable=tabCompletionMoveDown
flyline key bind Ctrl+p tabCompletionAvailable=tabCompletionMoveUp

eval "$(zoxide init bash)"
cat ~/.cache/terminal-sequences
